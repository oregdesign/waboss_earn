{
  "name": "waboss",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev",
    "build": "npm install && npm run build",
    "start": "npm --prefix waboss-backend start"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
